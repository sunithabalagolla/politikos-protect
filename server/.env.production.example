# Production Environment Variables
# Copy this to your Render dashboard as environment variables

NODE_ENV=production
PORT=5000

# MongoDB Atlas Connection String
# Get this from MongoDB Atlas → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/politikos?retryWrites=true&w=majority

# JWT Secret - Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-generate-a-random-one

# JWT Expiration
JWT_EXPIRE=7d

# Frontend URL (your Render frontend URL)
CLIENT_URL=https://politikos-frontend.onrender.com

# Optional: Email Service (for OTP verification - add later)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
